<div class="row justify-content-center py-3">
  <div class="col-md-4 card receipt text-uppercase shadow m-2">
    <div class="card-body">
      <div class="w-100 d-flex">
        <button class="btn d-print-none ml-auto" onclick="window.print()">
          <i class="fas fa-print"></i>
        </button>
      </div>
      <div class="text-center">
        <h1>Residencias 200</h1>
        <p class="small">esq. crucesitas a porvenir</p>
        <hr />
        <h4 class="mb-4"><%= expenditure.title %></h4>
      </div>
      <p><strong>ref:</strong> <%= expenditure.id%></p>
      <p><strong>fecha:</strong> <%= date %></p>
      <p><strong>pagado a:</strong> <%= expenditure.payto %></p>
      <p><strong>motivo:</strong> <%= expenditure.reason %></p>
      <p><strong>descripción:</strong> <%= expenditure.descrip %></p>
      <h4 class="mb-4">monto: $<%= expenditure.mount %></h4>
      <hr />
      <p class="text-justify small">
        <strong>nota:</strong> el monto se descontará al
        <strong>fondo de recuperación</strong> automaticamente.
      </p>
    </div>
  </div>
  <div class="col-12 d-flex justify-content-center d-print-none">
    <div class="col-md-4">
      <% if(user.privileges){ %>
      <a
        href="/expenses/edit/<%= expenditure.id %>"
        class="btn btn-primary my-4"
        >editar</a
      >
      <% } %>
      <a href="/" class="btn btn-success float-right my-4">siguiente</a>
      <% if(user.privileges){ %>
      <!-- delete -->
      <form
        class="text-center"
        action="/expenses/<%= expenditure.id %>?_method=DELETE"
        method="POST"
      >
        <input type="hidden" name="_method" value="DELETE" />
        <button type="submit" class="btn btn-outline-danger m-2">
          <i class="fas fa-times mr-1"></i>
          eliminar
        </button>
      </form>
      <% } %>
    </div>
  </div>
</div>
